"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Vendor
const chalk_1 = require("chalk");
// Services
const revision_slug_fetcher_1 = require("../revision-slug-fetcher");
class ProjectAddTranslationsFormatter {
    log(project) {
        const languages = project.revisions
            .filter((revision) => revision_slug_fetcher_1.fetchFromRevision(revision) !==
            revision_slug_fetcher_1.fetchFromRevision(project.masterRevision))
            .map(revision_slug_fetcher_1.fetchFromRevision)
            .join(', ');
        console.log(chalk_1.default.magenta('Adding translations paths', `(${languages})`));
        console.log('');
    }
}
exports.default = ProjectAddTranslationsFormatter;
