"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class DocumentJiptPathsFetcher {
    fetch(project, document, pseudoLanguageName) {
        return project.documents.entries
            .map(({ path }) => path)
            .map((path) => {
            const parsedTarget = document.target
                .replace('%slug%', pseudoLanguageName)
                .replace('%original_file_name%', path);
            return {
                documentPath: path,
                language: pseudoLanguageName,
                path: parsedTarget,
            };
        });
    }
}
exports.default = DocumentJiptPathsFetcher;
