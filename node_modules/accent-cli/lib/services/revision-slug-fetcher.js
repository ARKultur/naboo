"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchNameFromRevision = (revision) => {
    return revision.name || revision.language.name;
};
exports.fetchFromRevision = (revision) => {
    return revision.slug || revision.language.slug;
};
exports.fetchFromRevisions = (revisions) => {
    return revisions.map(exports.fetchFromRevision);
};
exports.fetchMasterFromProject = (project) => {
    const masterRevision = project.revisions.find(({ isMaster }) => isMaster);
    return exports.fetchFromRevision(masterRevision);
};
