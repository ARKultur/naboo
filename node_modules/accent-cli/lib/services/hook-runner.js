"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Vendor
const child_process_1 = require("child_process");
// Formatters
const hook_runner_1 = require("./formatters/hook-runner");
class HookRunner {
    constructor(document) {
        this.document = document;
        this.hooks = document.config.hooks;
    }
    /* eslint-disable @typescript-eslint/require-await */
    async run(name) {
        if (!this.hooks)
            return null;
        const hooks = this.hooks[name];
        if (hooks) {
            new hook_runner_1.default().log(name, hooks);
            hooks.forEach(child_process_1.execSync);
        }
        return this.document.refreshPaths();
    }
}
exports.default = HookRunner;
