import { flags } from '@oclif/command';
import Command from '../base';
export default class Export extends Command {
    static description: string;
    static examples: string[];
    static args: never[];
    static flags: {
        'order-by': flags.IOptionFlag<string | undefined>;
    };
    run(): Promise<void>;
}
